## Python 3.13 兼容核心依赖
## 原版本 (pandas 2.1.3 / scikit-learn 1.4.2 / matplotlib 3.8.3 / streamlit 1.33.0) 在 Cloud 默认 Python 3.13 上缺少轮子，导致源码编译和 numpy rc 依赖错误
## 说明：若强制使用 Python 3.12 可保留旧版本，但当前策略改为升级以直接兼容 3.13
streamlit==1.40.0
matplotlib==3.9.0
pandas==2.2.0         # >=2.2.0 提供 Python 3.13 轮子；注意其要求 numpy < 2
numpy==1.26.4         # 满足 pandas 2.2.0 的 >=1.26,<2 约束；Py3.13 有轮子
scikit-learn==1.6.0   # 1.6.0 起官方支持 Python 3.13
seaborn==0.13.2
plotly==5.18.0

# PDF处理
PyPDF2>=3.0.0

# AI大模型依赖
openai==2.7.1
requests>=2.31.0
pydantic==2.12.4
tqdm==4.67.1  # 固定版本，防止与其它文件出现 >= 旧版本冲突
distro==1.9.0

# Word报告生成
python-docx==0.8.11
reportlab==4.0.7
Pillow==10.2.0

# 数据处理和分析
openpyxl==3.1.2
xlrd==2.0.1
xlsxwriter==3.1.9

# 日志和调试
# 标准库 logging 无需安装；移除错误的 PyPI 占位包

# 机器学习模型
# 机器学习模型（可选：暂时注释以加快基础环境安装；聚类等核心功能使用 sklearn即可）
# xgboost==2.0.3
# lightgbm==4.3.0
# catboost==1.2

# 时间序列分析
statsmodels>=0.14.2  # 0.14.2+ 对 3.13 支持更完整
# prophet 构建时间长，且可能在 3.13 上拉取大量编译依赖；如非必须建议放入可选依赖或延迟安装
# prophet==1.1.5

# 优化
scipy==1.12.0
joblib==1.3.2

# 可视化扩展
altair==5.2.0
folium==0.16.0
wordcloud==1.9.3

# AI和NLP工具 (可选, 为加快基础运行已注释不必要/大体积依赖)
# 若需要深度学习/文本分析功能可按需取消注释并使用兼容版本
# tensorflow==2.16.1  # 2.15 对 Python 3.12 支持不完整，升级到 2.16.x（可选）
# torch==2.2.0        # 原来写作 pytorch，正确包名为 torch（可选）
spacy==3.7.4
nltk==3.8.1

# 工具类（修正 yaml -> PyYAML，移除重复/旧版本冲突）
PyYAML==6.0.1
# 已在上方固定 pydantic==2.12.4 与 tqdm==4.67.1，这里删除重复旧版本
pydantic-settings==2.1.0