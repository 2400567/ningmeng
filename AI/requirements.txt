## Python 3.11/3.12 兼容核心依赖（避开 3.13 + NumPy 2.0 早期构建问题）
## 若未来需要 NumPy 2.x：升级 pandas>=2.2.2 与 scikit-learn>=1.6.0 并同步 constraints。
streamlit==1.40.0
matplotlib==3.8.0        # 按用户建议选用稳定版本 3.8.x
pandas==2.2.0            # 需要 numpy <2.0
numpy==1.26.4            # 精确锁定，避免不同 patch 版本导致重复解析
scikit-learn==1.5.0      # 按建议回退到 1.5.0（依旧支持 Py 3.11/3.12）
seaborn==0.13.2
plotly==5.18.0

# PDF处理
PyPDF2>=3.0.0

# AI大模型依赖
openai==2.7.1
requests>=2.31.0
pytz>=2021.1
python-dotenv>=1.0.0      # 新增：用于本地加载环境变量（API keys 等）
pydantic==2.12.4
tqdm==4.67.1  # 固定版本，防止与其它文件出现 >= 旧版本冲突
distro==1.9.0

# Word报告生成
python-docx==0.8.11
reportlab==4.0.7
Pillow==10.2.0

# 数据处理和分析
openpyxl==3.1.2
xlrd==2.0.1
xlsxwriter==3.1.9

# 日志和调试
# 标准库 logging 无需安装；移除错误的 PyPI 占位包

# 机器学习模型
# 机器学习模型（可选：暂时注释以加快基础环境安装；聚类等核心功能使用 sklearn即可）
# xgboost==2.0.3
# lightgbm==4.3.0
# catboost==1.2

# 时间序列分析
statsmodels>=0.14.2  # 0.14.2+ 对 3.13 支持更完整
# prophet 构建时间长，且可能在 3.13 上拉取大量编译依赖；如非必须建议放入可选依赖或延迟安装
# prophet==1.1.5

# 优化
scipy==1.12.0
joblib==1.3.2

# 可视化扩展
altair==5.2.0
folium==0.16.0
wordcloud==1.9.3

# AI和NLP工具 (可选, 为加快基础运行已注释不必要/大体积依赖)
# 若需要深度学习/文本分析功能可按需取消注释并使用兼容版本
# tensorflow==2.16.1  # 2.15 对 Python 3.12 支持不完整，升级到 2.16.x（可选）
# torch==2.2.0        # 原来写作 pytorch，正确包名为 torch（可选）
spacy==3.7.4
nltk==3.8.1

# 工具类（修正 yaml -> PyYAML，移除重复/旧版本冲突）
PyYAML==6.0.1
# 已在上方固定 pydantic==2.12.4 与 tqdm==4.67.1，这里删除重复旧版本
pydantic-settings==2.1.0